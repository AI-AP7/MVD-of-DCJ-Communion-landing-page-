
export interface Speaker {
  name: string;
  role: string;
  title: string;
  imageUrl: string;
}

export interface EventDetails {
  jurisdiction: string;
  title: string;
  subtitle: string;
  date: string;
  location: string;
  address: string;
  host: string;
  hashtag: string;
}
